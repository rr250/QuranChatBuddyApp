# Firebase Configuration Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

EXPO_PUBLIC_FIREBASE_API_KEY=your_api_key_here
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
EXPO_PUBLIC_FIREBASE_DATABASE_URL=https://your_project.firebaseio.com/
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
EXPO_PUBLIC_FIREBASE_APP_ID=your_app_id
EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# Google OAuth Client IDs (Firebase / Google Cloud Console)
# WEB client is required for Firebase Auth ID tokens — do not use Android client ID here.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your_web_client_id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your_android_client_id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your_ios_client_id.apps.googleusercontent.com
# After setting IDs, add the app redirect URI to the WEB client's Authorized redirect URIs.
# Run the app once in dev — Metro logs: [Google Auth] redirect URI: quranchatbuddy://oauthredirect
# Also enable Google sign-in in Firebase Console → Authentication → Sign-in method.
# DEVELOPMENT ONLY — Direct OpenAI fallback (bypasses Cloud Functions when they fail in dev).
# WARNING: EXPO_PUBLIC_ variables are inlined into the JS bundle at build time and are
# trivially extractable from any APK/AAB. Never set this in production EAS secrets.
# In production, all AI calls go through Firebase Cloud Functions only.
# EXPO_PUBLIC_OPENAI_API_KEY=sk-your_openai_api_key_dev_only

# RevenueCat (subscriptions / paywall)
# Test key is used in __DEV__ mode. Platform keys are used in production builds.
EXPO_PUBLIC_REVENUECAT_TEST_API_KEY=your_revenuecat_test_api_key
EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY=your_revenuecat_android_api_key
EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=your_revenuecat_ios_api_key

# Add other sensitive configuration here
# API_SECRET_KEY=your_secret_key
# DATABASE_PASSWORD=your_db_password

# Daily quiz question bank (optional override). If unset, built from EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET + DailyQuiz path.
# Use tokenless ?alt=media URL only after App Check is unenforced for Storage (see Firebase Console → App Check).
# EXPO_PUBLIC_QUIZ_QUESTIONS_URL=https://firebasestorage.googleapis.com/v0/b/your-bucket/o/DailyQuiz%2Fislamic_quiz_500_real.json?alt=media

# Optional salt for hashing device IDs (anti-abuse / device-bound anonymous auth)
# EXPO_PUBLIC_DEVICE_HASH_SALT=your_random_salt_here